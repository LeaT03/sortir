<nav class="navbar navbar-expand-lg" style="background: cornsilk">
    <div class="container-fluid">
        <a href="{{ path("app_main") }}" title="Retour Accueil">
            <img src="{{ asset('img/sortir2paris-fond-blanc-small.png') }}" alt="Sortir Logo" style="width: 200px">
        </a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse"
                data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent"
                aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <ul class="navbar-nav ms-auto mb-2 mb-lg-0 ">
                {% if is_granted("ROLE_ADMIN") %}
                    <li class="nav-item">
                        <a class="nav-link" href="#" title="Ville">Ville</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="#" title="Campus">Campus</a>
                    </li>
                {% endif %}
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('app_main') }}" title="Accueil">Accueil</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('app_profil_participant') }}" title="Profil">Mon profil</a>
                </li>

                {% if app.user %}
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('app_logout') }}" title="Déconnexion">Se déconnecter</a>
                    </li>
                {% else %}
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('app_login') }}" title="Login">Se Connecter</a>
                    </li>
                {% endif %}
            </ul>
        </div>
    </div>
</nav>
