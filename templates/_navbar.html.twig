<nav class="navbar navbar-expand-lg">
    <div class="container-fluid">
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse"
                data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent"
                aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                {% if is_granted("ROLE_ADMIN") %}
                    <li class="nav-item">
                        <a class="nav-link" href="#" title="Ville">Ville</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="#" title="Campus">Campus</a>
                    </li>
                {% endif %}
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('app_main') }}" title="Campus">Accueil</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('app_profil_participant') }}" title="Profil">Mon profil</a>
                </li>

                {% if app.user %}
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('app_logout') }}" title="Déconnection">Se déconnecter</a>
                    </li>
                {% else %}
                    {#                <li class="nav-item">#}
                    {#                    <a class="nav-link" href="{{ path('app_register') }}" title="Create your account">Register</a>#}
                    {#                </li>#}
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('app_login') }}" title="Login">Se Connecter</a>
                    </li>
                {% endif %}
            </ul>
        </div>
    </div>
</nav>
{% if app.user %}
    <h5 style>Date du jour: {{ "now"|date("d/m/Y") }}</h5><br>
    <h5>Participant: {{ app.user.prenom }}</h5>
{% endif %}